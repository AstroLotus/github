#Let us begin our automation journey
result = template.format(*parameters)

#strings can be sliced like any other list. 
'[word]'[0:2] # will return 'wo'

.splitlines() #can be used to seperate lines by newline characters
.upper() .lower() #can be used to manipulate captalization
.replace() # for easy replacement